/**
 * Schema is managed declaratively by Convex (convex/schema.ts).
 * These functions are no-ops kept for call-site compatibility.
 */
export function runMigrations(): void {}
export function ensureSchema(): void {}
